import OrgLoginForm from "@/components/OrgLoginForm";

export default function OrganisationLoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-100 via-cyan-100 to-emerald-100">
      <OrgLoginForm />
    </div>
  );
}
